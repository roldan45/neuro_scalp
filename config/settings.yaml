exchange:
  name: "okx"
  testnet: true
  pairs: ["BTC-USDT-SWAP", "ETH-USDT-SWAP"]
  leverage: 10

risk:
  max_drawdown_daily: 0.03 # 3%
  max_position_size_usd: 10000
  stop_loss_pct: 0.002 # 0.2%
  daily_kill_switch: true

ml:
  retrain_interval_minutes: 60
  lookback_window: 100